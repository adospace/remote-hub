using Xunit;

namespace RemoteHub.Tests;

// TODO(Implement): round-trip AppSettings using a temp directory; assert defaults are sane and
// ConnectionsFilePath is never null after Load().
public class SettingsServiceTests
{
    [Fact]
    public void Scaffold_Placeholder()
    {
        Assert.True(true);
    }
}
