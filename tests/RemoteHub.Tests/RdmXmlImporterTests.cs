using Xunit;

namespace RemoteHub.Tests;

// TODO(Implement): assert folder nesting from Group paths, field mapping, non-RDP entries
// skipped, connection count, and that NO password data leaks into the result. Load the sample
// from docs/sample-rdm-export.xml (copy to output or embed as a constant).
public class RdmXmlImporterTests
{
    [Fact]
    public void Scaffold_Placeholder()
    {
        Assert.True(true);
    }
}
