using Xunit;

namespace RemoteHub.Tests;

// TODO(Implement): build a nested document, SaveAsync to a temp path, LoadAsync back and assert
// deep equality; assert that loading a missing file returns an empty document.
public class ConnectionStoreTests
{
    [Fact]
    public void Scaffold_Placeholder()
    {
        Assert.True(true);
    }
}
